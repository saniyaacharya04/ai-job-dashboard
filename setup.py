from setuptools import setup, find_packages

setup(
    name="ai_job_dashboard",
    version="0.1.0",
    description="Intelligent Job Search & Candidate Matching Platform",
    packages=find_packages(),
    python_requires=">=3.10",
)
